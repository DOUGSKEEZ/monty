[Unit]
Description=Update yt-dlp to latest version
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
ExecStart=/home/monty/monty/scripts/yt-dlp-update.sh
User=root
StandardOutput=journal
StandardError=journal

# Don't fail if already running
# (prevents overlap if manual run during timer)
